<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Users</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <style>
        .font {
            color: rgba(0, 0, 0, 0.65) !important;
            font-weight: 600;
        }

        p {
            margin-bottom: 0;
        }
    </style>
</head>

<body>
    <div class="container">
        <h3 class="font text-center m-2">Logged Users</h3>
        <ul class="list-group font" id="lgroup">
            <% if(data && data.length>0){%>
                <% data.forEach((val)=>{%>
                    <li class="list-group-item">
                        <div class="d-flex justify-content-around">
                            <p>
                                <%= val.first_name+" "+val.last_name %></p>
                            <form action=" delete/<%=val._id.toString() %>" method="post"><button style="background-color: transparent;border: none;"><i class="fa fa-trash-o"
                                            style="font-size:26px;color:red"></i></button>
                                    </form>
                        </div>
                    </li>
                    <br />
                    <%})%>
                        <% }else{%>
                            <li class="list-group-item">No Users Found</li>
                            <%}%>
        </ul>
        <form action="/" method="post" class="d-flex justify-content-center">
        <button class="btn btn-outline-primary w-50 text-center">Login</button>
    </form>
    </div>
</body>

</html>